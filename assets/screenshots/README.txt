Claude Science UI screenshots referenced from Connecting-Claude-Science-to-AWS.md
(Step 4). Filenames are load-bearing — keep them stable if you re-capture:

  cs-compute-ssh-alias.png     — Compute → Add SSH host (the host-alias form)
  cs-credentials-aws.png       — Credentials → AWS (the access-key entry)
  cs-compute-scratch-root.png  — Compute → <host>: the pinned Scratch root field
